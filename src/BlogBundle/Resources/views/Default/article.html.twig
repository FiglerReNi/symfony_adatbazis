{% extends 'base.html.twig' %}

{% block title %}
    {{ article.title }}
{% endblock %}

{% block body %}
    <h1>{{ article.title }}, {{ article.createdAt|date('Y/m/d H:i:s') }}</h1>
    <p>{{ article.description }}</p>
    <p>{{ article.content }}</p>
{% endblock %}